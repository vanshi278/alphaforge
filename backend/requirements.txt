# ---- Web / async API ----
fastapi
uvicorn[standard]
websockets
python-dotenv

# ---- Data / storage ----
pandas
numpy
psycopg2-binary
redis
sortedcontainers
yfinance          # historical OHLCV (no API key); Phase 1 source

# ---- Quant / scientific ----
scipy
statsmodels
scikit-learn
cvxpy
matplotlib        # equity-curve / drawdown plots (backtest results)

# ---- ML / forecasting (Phase 4) ----
lightgbm          # gradient-boosted trees (falls back to sklearn if libomp missing)
shap              # model interpretability (falls back to feature_importances_)
# torch           # optional LSTM/GRU sequence model
# alphalens-reloaded

# ---- Dev / test ----
pytest
